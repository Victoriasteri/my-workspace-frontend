# API Configuration
# 
# Set the base URL for your API backend.
# For local development, this defaults to http://localhost:3000 if not set.
# For production, set this to your production API URL.
#
# Copy this file to .env.local and update the values for your local development.
# The .env.local file is gitignored and will not be committed.

# Example for local development (defaults to http://localhost:3000 if not set):
# NEXT_PUBLIC_API_URL=http://localhost:3000

# Example for production:
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com

NEXT_PUBLIC_API_URL=
